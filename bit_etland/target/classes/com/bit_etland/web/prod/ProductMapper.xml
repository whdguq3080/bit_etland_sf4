<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit_etland.web.prod.ProductMapper">
<resultMap id="result" type="hashmap">
  <id property="productsID" column="PRODUCT_ID" />
  <result property="productName" column="PRODUCT_NAME"/>
  <result property="supplierID" column="SUPPLIER_ID"/>
  <result property="categoryID" column="CATEGORY_ID"/>
  <result property="unit" column="UNIT"/>
  <result property="price" column="PRICE"/>
  <result property="rownum" column="ROWNUM"/>
</resultMap>
<select id="selectProductlists" resultType="result" parameterType="com.bit_etland.cmm.Proxy">
 	SELECT 	@ROWNUM:=@ROWNUM+1 AS rownum,  
 			PRODUCT_NAME productName,
 			UNIT,
 			PRICE,
 			CATEGORY_NAME categoryID
	FROM PRODUCTS JOIN CATEGORIES ON PRODUCTS.CATEGORY_ID LIKE CATEGORIES.CATEGORY_ID
	WHERE PRODUCT_NAME LIKE #{value}
	OR UNIT LIKE #{value};
  </select>	  
  <select id="countProduct" resultType="int">
    SELECT COUNT(*) FROM PRODUCTS
      </select>
 <select id="countProducts" resultType="int"  parameterType="string">
    SELECT COUNT(*)
	FROM PRODUCTS JOIN CATEGORIES ON PRODUCTS.CATEGORY_ID LIKE CATEGORIES.CATEGORY_ID
	WHERE PRODUCT_NAME LIKE #{value}
	OR UNIT LIKE #{value};
      </select>
</mapper>